# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository (`jakober/Cms`) is empty aside from this file — it contains no source code, build configuration, or documentation yet. There are no build, lint, or test commands to run, and no architecture to describe.

## When Code Is Added

Once the project takes shape, regenerate this file (e.g. with `/init`) so it documents:

- The commands actually used to build, lint, test, and run the project (including how to run a single test).
- The high-level architecture — the parts that require reading multiple files to understand, not a file-by-file listing.
- Any project-specific conventions established in the codebase.

Do not assume a tech stack, framework, or project layout based on the repository name alone; verify against the code that exists at that time.
